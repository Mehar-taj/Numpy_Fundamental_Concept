#Converting a list into an array without declaring the list
import numpy as np
arr = np.array([1,2,3,4,5])
arr1 = np.array([6,7,8,9,10])
# finding max of the array.
print(arr.max()) 
# finding min of the array.
print(arr1.min())
#Finding the avg of the array
print(arr.mean())
'''
# Numpy stands for Numarical python.
# Numpy is the python libray used for working with arrays.
# Arrays is collection of objects.

a=[1,2,3,4,5] add +1 to each element of the array.
sum=0
for i in a:
    sum+=i
    print(i+1)
'''
#steps to import numpy
import numpy as np
a = np.array([1,2,3,4,5])
print(a+1)# it will add 1 to each element of the array.